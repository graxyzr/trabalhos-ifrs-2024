<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Empregado</title>
    <link rel="stylesheet" href="/styles/styles.css"> <!-- Inclua seu CSS aqui -->
</head>

<body>
    <header>
        <h1>Editar Empregado</h1>
    </header>
    <main>
        <form action="/empregados/{{empregado.id}}" method="post">
            <label for="nome">Nome:</label>
            <input type="text" id="nome" name="nome" value="{{empregado.nome}}" required>

            <label for="salario_bruto">Salário Bruto:</label>
            <input type="number" id="salario_bruto" name="salario_bruto" step="0.01" value="{{empregado.salario_bruto}}"
                required>

            <label for="departamento">Departamento:</label>
            <select id="departamento" name="departamento" required>
                <option value="1" {{#if (eq empregado.departamento 1)}}selected{{/if}}>Administrativo</option>
                <option value="2" {{#if (eq empregado.departamento 2)}}selected{{/if}}>Designer</option>
                <option value="3" {{#if (eq empregado.departamento 3)}}selected{{/if}}>Contábil</option>
                <option value="4" {{#if (eq empregado.departamento 4)}}selected{{/if}}>Fábrica</option>
            </select>

            <button type="submit">Atualizar</button>
        </form>
        <a href="/empregados">Voltar à lista de empregados</a>
    </main>
</body>

</html>